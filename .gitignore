.DS_Store

# Internal source notes — not for the public repo
/project descrips/
portfolio_site_prompt.md

# Secrets
.env
.env.local

# Dependencies — regenerated by `npm install` from package.json, never committed
node_modules/
